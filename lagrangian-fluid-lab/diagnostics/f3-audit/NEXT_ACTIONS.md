# F3 recovery checkpoint

No solver or F3 Python/PartVTK process was present at checkpoint. The active CPU accounting window has been closed prospectively; historical reservations remain unchanged. CPU budget extension is pending an explicit owner answer, not implied by automatic goal continuations.

Completed: CELL3 GenCase at 0.010/0.0075/0.006 m, each represents 14.58 kg; zero GenCase normals; native driving file parser passes, coverage 0–8.35 s. These are preprocessing results, not runtime qualification.

Resume by checking the approved resource limits and regenerating RESOURCE-LEDGER. The counter includes five direct launches (41 known qualification attempts, 15 remaining). Unknown direct durations use explicit conservative charges, not fabricated measurements. Confirm no duplicate attempts are imported if migrating the supplemental records.

Next scientific work: register temporal/output controls and frame semantics from the reviewer package; run a single guarded canary using CELL3 inputs via the existing attempt runner, with UUID selection, one-second GPU guard, single solver lock, input checks and durable accounting. The old branch runner's F3 local cap must be reconciled with the adopted shared parent cap. Do not launch raw solver commands. Evaluate registered metrics with mass-weighted outside/missing categories and actual timestamps. Complete the remaining reference, endpoint and interior-point gates before independent development production. Material reference and two routes × three seeds remain outstanding.

The earlier 8-row partition manifest is not generated independent data. The original 0.006 retry completed in 103.765228 seconds but used a different initial mass. The 3.1-hour infeasibility claim and original TV acceptance claim have been withdrawn.

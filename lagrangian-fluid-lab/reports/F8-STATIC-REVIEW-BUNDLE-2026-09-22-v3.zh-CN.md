# F8 static review bundle v3（2026-09-22）

v3 只做 hash-closure 版本升级：在 v2 的 startup oracle、observation parser、
13+2 matrix 和非 admission 边界之上，绑定当前的
`body-force-driven oscillatory viscous channel` candidate card v2。

F8 仍等待正式 Core scope ruling；`qualification_credit=0`。没有写
Definition/control，也没有执行 GenCase、native decode、solver、GPU、queue、
registry、ledger 或 training。旧 candidate card 和 bundle v1/v2 全部保留为
历史证据。

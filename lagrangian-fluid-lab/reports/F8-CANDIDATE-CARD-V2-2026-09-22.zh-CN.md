# F8 candidate card v2（2026-09-22）

v2 是对 F8 候选身份和命名的静态修订，不覆盖 v1。机制名称统一为
`body-force-driven oscillatory viscous channel`；`pressure-driven` 只保留为
常密度连续体下的等价解释，不作为已验证的数值输入语义。

当前仍等待正式 root scope ruling：PLAN 的 Core 门槛要求三个不同机制家族，
但项目定位强调自由表面流动。若按机制家族解释，F8 可继续作为第三家族候选；
若要求自由表面，F8 转为 Core 之后扩展。v2 不替 root 选择任何一项。

该 card 绑定 static review bundle v2、startup oracle/parser 基础设施，仍为
zero credit；没有写 Definition/control，也没有执行 GenCase、native decode、
solver、GPU、queue、registry、ledger 或 training。

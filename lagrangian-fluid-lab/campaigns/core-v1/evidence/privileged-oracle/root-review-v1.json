{
  "findings": [
    {
      "issue": "commit overwrites all public updater outputs with native next state",
      "required": "retain shared-active updater output; explicitly isolate privileged lifecycle handling"
    },
    {
      "issue": "position and velocity RMSE hardcoded zero",
      "required": "measure committed versus reference; inject defective updater and verify nonzero final scores"
    },
    {
      "issue": "strict zero equality conflates floating-point accuracy with execution completion",
      "required": "separate complete execution from registered oracle accuracy tolerance"
    },
    {
      "issue": "oracle increment must not cancel accumulated state error",
      "required": "generate increments from consecutive native reference states, propagate committed state independently"
    }
  ],
  "jobs_launched": false,
  "runner_sha256": "1e366bbb8747eb4799173be856be1b790d8f14076ec0e6f593b882980489b223",
  "status": "changes_required_before_launch"
}

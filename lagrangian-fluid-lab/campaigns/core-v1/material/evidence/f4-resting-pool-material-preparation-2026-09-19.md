# F4 material overlay preparation

This record is preparation only. It makes no qualification claim, writes no ledger, and starts no job.

The real resting-pool canary is `/home/jade/Projects/DualSPHysics/lagrangian-fluid-lab/campaigns/core-v1/runtime/attempts/f4-resting-pool-canary-001/20260919T172542-d49b17d63c4f/product/trajectory.h5` (`sha256 892583601f507a2f66977a337718e29e108196aa3d6e83c8a9c3d59d634b6be6`), prepared from `/home/jade/Projects/DualSPHysics/lagrangian-fluid-lab/campaigns/core-v1/cfd/prepared/F4_resting_pool_center_medium_canary/prepared.json` (`sha256 170aa5facc7f1d08688a3f7ef01af45cbc4226cc660aa91a4e89f74c03893abd`). Its continuous definition is q=0.5, drop box low `[0.36, 0.12, 0.4]`, size `[0.26, 0.16, 0.14]`, destination pool low `[0.0, 0.0, 0.0]`, size `[1.2, 0.4, 0.18]`, interface z=0.18 m. It contains 16 frames through 0.3000032376922204 s. The source recipe is `CORE_F4_mdbc_native_cell_center_v1`; the material overlay binds the migration recipe `F4_resting_pool_mdbc_native_v2`. The canary remains `right_censored_or_unresolved` and is not a T2 source.

The CPU submission file is [`f4-resting-pool-material-canary-cpu-job.json`](f4-resting-pool-material-canary-cpu-job.json), mirrored at `campaigns/core-v1/material/jobs/core-f4-material-resting-pool-canary-s2.json` for the central runtime. It invokes `scripts/core_material.py --family f4 --q 0.5 --seeds 512 --substeps 2 --neighbour-variant baseline24` against that real H5. Its acceptance rules preserve NaN/right-censored events and forbid a qualification result.

The existing F4 CFD matrix has 15 cells: 13 spatial cells and two center temporal controls. The 24 resolution/substep rows in the prior 33-row material template all have a spatial source cell. The four cadence rows do not have exact sources: F4 center cell-14 is native `.004 s`, while the template asks for `.002 s`; q=1 has no dense parent. The five 4096-seed rows have `.0075 m` source cells, but the material overlays are still pending. The full mapping and hashes are in [`f4-resting-pool-33-to-15-matrix-review-2026-09-19.json`](f4-resting-pool-33-to-15-matrix-review-2026-09-19.json).

Synthetic State-H5/event artifacts copied from `/tmp` are in [`f4-interface-validation-2026-09-19`](f4-interface-validation-2026-09-19). The archive manifest labels them synthetic interface tests only; they cannot qualify F4.

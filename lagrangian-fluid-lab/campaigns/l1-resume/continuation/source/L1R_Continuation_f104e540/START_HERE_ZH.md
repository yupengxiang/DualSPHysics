# L1-R 续执行指令：不重新立项，不以Q2单例失败结束

基线：`f104e5409cec2c8dd4043f93ffc4f6c7cda96646`。日期：2026-09-09。

请在既有 `codex/lagrangian-fluid-exploration` 分支上继续原已采纳L1/L1-R范围内的工作。先读取本包 `CONTINUATION_PLAN_ZH.md` 和 `REVIEW_FINDINGS_ZH.md`，核对本地所有者采纳记录、原活动起始时间、剩余资源和输入状态。此正文是同范围执行修订，不是新的所有者签名、云端验收或预算归零。无本地证据支持时不伪造授权；已经涵盖的操作不要逐case重复申请。

## 立即推进的两条路径

1. **官方已有轨迹变成实际参考对照**：Q1现有601帧原始输出复用，补齐正确几何/时域的分块转换、原生排除、身份/质量/墙面和对应外部观测检查。不要重复跑一遍Q1，不要用L1小水箱的硬编码壁面去审计官方大水槽。官方仍只是执行路径成功，物理资格未完成。
2. **Q2配方和底壁异常分离**：Q2当前不是完整官方数值配方。官方记录是StepAlgorithm=2、Visco=0.01、DensityDT=3；Q2是1、0.08、2。记录完整差异和实际运行参数。先重评已有Q2的真实越界深度/位置/累计身份，再在合规几何上构造“官方数值策略派生”候选；它是新配方，不冒称仅改变一个参数的因果实验。

先修实际 `finite_wall_audit.py` 中跨容差带的漏检：同一直线路径加保存帧不能让跨墙事件消失。将几何跨平面与超出容差带两个量分开；保留原判定和重评差异。`runtime_domain=null` 时域越界必须是unknown/not_checked，不是证实为0。修审计不要求重跑CFD。

## 连续执行规则

- 沿用原L1剩余额度，不重置64 GPU·h、512 CPU core·h、56资格attempt、40试产attempt、12训练attempt、32材料组、512 GiB存储和原168小时有效期。先汇总L1/L1-R全部attempt，包括失败和guard停止。
- GPU4–7、UUID allowlist；GPU0–3保护；整个活动最多一个solver。启动至少6144 MiB空闲且预估峰值后留4096 MiB，运行每秒检查，低于4096 MiB只终止本活动进程组。保留共机负载，不宣称零性能影响。不自动以CPU替代完整资格矩阵。
- 官方既有数据审计、初态/几何CPU研究、材料/学习接口工作可继续；GPU资源不可用不是这些工作的共同阻塞。
- Q2最多6次有依据的新增诊断solver（从原余额扣除），先短窗0.6 s，通过后才完整1.5 s；几何必须先核实法向、ghost与规范壁面。NoPenetration不盲开，必须有实际版本语义与影响审计。
- Q2有效候选通过后进入原Q3空间/时间与内部点，满足对应门后自动8例试产、扩展到24–32例、材料参考及两路线各3种子；不要求T2/F6/全部压力Gold同时通过才允许T1-only开发数据。
- Q2有限路径耗尽时，不以“无T1”否决官方模板派生路径和预设F3后备；按任务图继续，至少给后备真实的执行/验收终态或资源/资产阻塞证据。
- 不再新写一个永远把development=false写死的总控制器。实现型缺口是范围内待办，不是必须等待reviewer的外部障碍。
- 阶段checkpoint可以提交推送，但不结束活动；最终报告仅在得到实际开发产品、全部适用分支被证据否定、原预算/期限耗尽或真实外部阻塞时形成。
- 不修改上游/vendor，不覆盖旧失败证据，不删除唯一原始数据，不强推/自动合并/正式发布，不虚报历史对话读取或reviewer回写。

下一次总结首先回答：取得了什么生产能力；若没有，是哪个实际依赖阻塞，官方路径和F3是否已执行；不要只列测试数和一个新的Q2失败。

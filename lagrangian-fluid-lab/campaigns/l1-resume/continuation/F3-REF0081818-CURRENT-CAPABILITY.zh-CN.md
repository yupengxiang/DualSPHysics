# F3-REF0081818 当前能力与终态交接

记录：2026-09-13。状态：**有界活动以负结论收口**。数值参考配方和 32 个独立开发案例已经真实生成；材料候选和两条学习路线也真实执行并完成评估。材料 T2 与模型正式资格均未通过，且父 CPU 保守上界已超过上限、训练次数已耗尽，因此不再启动新的计费任务。

## 可复现的 F3 数值参考配方

- 配方：`F3_CELL3_NS_visco1_native_nopen_revision075_ref0081818`。
- 生产分辨率：`dp=0.0075 m`；参考阶梯：`0.010 / 0.0075 / 0.006 m`。历史 `0.015 / 0.03 m` 只作为失败证据保留。
- 控制范围：驱动幅值 `[0.9, 1.1]`，独立内部点 `0.97`；幅值只作用于已登记扰动控制，不缩放重力或整份 CSV。
- 时间窗：`0–8.35 s`；数值输出/评分间隔 `0.01 s`；源数据实际末时刻约 `8.350012828 s`。
- 坐标系：固定槽体计算坐标；这是规定平移/角加速度输入下的数值任务，不宣称惯性世界系真值。
- 可评分任务：零驱动稳定性、输出插值控制、时间控制、三分辨率名义空间参考、低/高端点和 `0.97` 内点空间检查；开发数据上固定最终步的 16 例留出 rollout，检查身份/质量/硬墙与穿越；材料上 512 种子首次通过、终点、停留区间及路径诊断。

配方 gate 和完整评分报告均为 `passed`，但 gate 的范围明确不包含材料或学习正式资格：

- `F3-075-REF0081818-GATE.json`（SHA256 `cd250459e7babe619c8fe320c652618c4383b4d481985df9a2d5e240cfd687d4`）
- `F3-075-REF0081818-SCORES-633a9f1c021fcbbaa2fa5a8a85f204753b6a801f7109003538f0c92a39425fa3.json`（SHA256 `633a9f1c021fcbbaa2fa5a8a85f204753b6a801f7109003538f0c92a39425fa3`）

## 独立开发数据

完整开发契约包含 32 个不同控制案例：16 train、4 validation、12 public development test。每个案例均有绑定的控制 CSV、输入审计、原生时间步、solver 日志和 HDF5；HDF5 均为 836 帧、34560 粒子轴、0–8.35 s。数据契约状态 `passed`，但仍是 development，未发布为 hidden test 或正式产品。

证据：`F3-REF0081818-TRAINING-DEVELOPMENT-FULL.json`（SHA256 `61a1a56f53bebe7edae6955b0469e4ee9cd1750b7388755c9d43d0407a0390db`）。

## 材料闭环

两个名义候选真实完成：`REF008-0075-NOMINAL-s2` 和 `REF008-0075-NOMINAL-s4`，各为 512 个固定种子、完整 0–8.35 s 轨迹。两者的子步敏感性比较已落盘：两个来源半区的 unknown 上界约 `0.0586–0.0625`，终点质量界差约 `0.0586–0.0664`，平均停留时间界差约 `0.208–0.276 s`，超过预先登记的候选预算；由于墙/支撑失败，不能计算完整路径统计。

已登记的 `REF008-0075-CADENCE-s4` 配置两次真实超时（900 s、1500 s），没有把超时改写成通过。材料所有结果保持 `candidate_only=true`、`qualified_T2_macro=false`、`qualified_T2_path=false`、`formal_release=false`。

证据：`F3-REF0081818-MATERIAL-COMPARISON-NOMINAL-SUBSTEP.json`（SHA256 `8246127d5056a2e7c39575715a8fd64616a0d7b7818da3b893d5673722803439`）及 `F3-REF0081818-MATERIAL-ARCHIVE-INDEX.json`。

## 学习闭环

两条路线 `particle_mlp`、`local_interaction` 各 3 个种子（17、29、43）均实际达到 16384 步。每个最终权重都完成 16/16 留出评估回放，总计 96 个案例；通过数 `0`，失败数 `96`。失败不是评估缺失：面板记录的是约 `0.05–0.11 s` 开始的 closed-face hard-wall/crossing，所有失败案例保留在分母中，没有模型排名或发布。

并行执行已按所有者授权落地：训练授权 GPU `0–7`，最大并行 8，同卡上限 2；四个逻辑运行在同一启动窗口并行使用 GPU 0–3，恢复/后续运行使用 GPU 4、5。CPU 亲和性分开绑定；没有终止外部进程。训练计划中 seed17 的最终有效工件来自资源策略等价的 R8 计划，交接中已明确标注，未冒充 R9 主计划。

证据：`F3-REF0081818-TRAINING-CLOSURE.json`（SHA256 `29991a5fe01b1b64fa3d624cd7582f7e44858a55c55f0fdaefc43235e53772e1`）及 `F3-075-REF0081818-TRAINING-PARALLEL-AUTHORIZATION.json`。

## 资源与终止理由

冻结后的 `RESOURCE-LEDGER.json` 显示：

- qualification `75/80`，剩余 5；development `32/40`，剩余 8；material configurations `16/32`；training `12/12`，剩余 0。
- GPU 计费 `17.7132/64 h`。
- CPU 保守上界 `911.958/896 core·h`，已超过父上限；活动窗口已关闭，截止时间仍为 `2026-09-16T00:00:00Z`。

因此本活动满足附件计划的终止谓词：可用模型产品没有完成，但所有已适用的有界分支都有实际终态，且继续材料/训练会触碰真实父资源限制。这里的“完成”是**负结论闭环**，不是材料或模型正式发布。

机器可审计总记录：`F3-REF0081818-CAMPAIGN-CLOSEOUT.json`（SHA256 `0be3d733c2af36ad16d32d302a0af2f73694e9587983b66d5058456c59a7a56f`）。附件 ZIP 仅作为规划与验证说明，不能替代所有者授权；本交接只引用本地实际执行记录。

若要取得通过 T2 或模型质量门的正产品，下一步必须由所有者另行批准新的父 CPU/训练预算并建立新的有界计划；现有负结论和失败记录不能被改判。

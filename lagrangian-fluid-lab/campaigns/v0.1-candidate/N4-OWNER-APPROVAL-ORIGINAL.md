**我明确批准本轮 N4 的新增计算预算与执行范围。**
以 `6db8158` 中已经审阅的 N4 计划为准，允许累计不超过 **0.5 GPU·h，即1800 GPU设备秒**，最多启动 **4次新的 solver attempt**。该预算是四例合计，不是每例各0.5 GPU·h。
仅运行 **h09 coarse、h09 medium、h11 coarse、h11 medium**：h09＝0.414 m，h11＝0.506 m；coarse粒距＝0.035 m，medium粒距＝0.024 m。保持已锁定的CFL＝0.1、1.5秒物理时域、0.001秒目标保存间隔，以及其余已审阅配置不变。
仅使用当前空闲、UUID核验通过的 **GPU 4–7**，不使用GPU 0–3，不中断任何既有任务。沿用既定批次锁、输入哈希绑定和单次441秒超时限制；失败、中断和超时均计入attempt次数与预算。不得自动启动第五次求解。
保存本授权原文及结构化授权记录，将其摘要绑定到批次和attempt。批准后直接执行，不再等待一轮同范围的技术审阅。若输入摘要与审阅版本不一致、设备不可用或预算条件不满足，记录具体原因，不绕过保护机制。
完成求解后，继续完成转换、原生排除记录核对、逐帧身份与质量审计、21个注册时刻的分辨率比较，以及最终交接。失败案例保留并如实报告；后处理失败优先修复后处理，不重复求解。
\*\*本授权仅用于补齐对照证据。\*\*不授权F6、G4训练、development tranche或正式发布；不改变TV、COM、q90判据。H10粗→中 `TV=0.056904761904761875 > 0.05` 的失败结论必须保留，不得用两个端点通过覆盖它。
将实际结果、资源账本、审计和结论提交并推送到现有探索分支；不修改上游求解器目录，不自动合并主线。

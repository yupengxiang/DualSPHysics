# W00 保护性盘点结论

- 第一轮证据基线锁定为 `0329009f469e7778cdbf6fe1fd09df2d5a13ea69`。
- 第二轮仍在独立的 `lagrangian-fluid-lab/` 内工作，不修改父仓库原有源码和案例。
- 第一轮 `runs/`、`runs-official/`、`data/`、`data-official/` 视为只读证据；第二轮数据写入 campaign 专属目录。
- 计划全集当前包含 30 个第一轮案例，而不是从成功 audit 反推案例全集。
- 日志证据确认其中 24 个为 3D，6 个官方接口探针为 2D；后者不能作为三维验证证据。
- 实际数据来自官方 DualSPHysics 5.4.355 二进制；源码快照 5.4.351 不被用来替代二进制行为证据。
- 盘点时 GPU 0–3 有外部负载，GPU 4–7 空闲。后续使用 UUID 白名单，并在每批启动前重新检查。
- `/home` 约有 9.2 TiB 可用空间；本 campaign 的新增数据硬上限为 1.5 TiB，但这不是必须消耗的额度。

机器可读证据见 `w00-inventory.json` 和 `case-registry.json`。盘点文件中的 Git dirty 状态对应本 W00 自身尚未提交的新增文件；本结论提交后再开始 W01，保证工作包边界清楚。
